const User = require('./User');
const Trades = require('./Trades');
const Settings = require('./Settings');
const Messages = require('./Messages');

module.exports = { User, Trades, Settings, Messages };
